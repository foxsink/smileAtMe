{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev-docker": "rm -rf /tmp/nitro && nuxt dev",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "firstrun": "yarn prisma generate && yarn prisma migrate dev && yarn prisma db seed",
    "dev-all": "yarn && yarn firstrun && yarn dev"
  },
  "prisma": {
    "seed": "ts-node --compiler-options {\"module\":\"CommonJS\"} prisma/seed.ts"
  },
  "devDependencies": {
    "@types/node": "^18.11.19",
    "nuxt": "^3.1.2",
    "prisma": "^4.10.0",
    "sass": "^1.58.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@prisma/client": "^4.10.0",
    "nuxt-swiper": "^1.0.0"
  }
}
